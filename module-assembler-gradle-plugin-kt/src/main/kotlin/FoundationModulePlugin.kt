import org.gradle.api.Project

class FoundationModulePlugin : AndroidPlugin() {
    override fun addPlugins(project: Project) {
        super.addPlugins(project)
    }

    override fun onApply(project: Project) {
        super.onApply(project)
    }
}