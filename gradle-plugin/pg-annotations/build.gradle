plugins {
    id 'io.github.jamesfchen.module-publisher-plugin' version '1.4.1'
}
//复核组件场景下，android项目implementation gradle-plugin项目中的模块，kapt {groupId}:{name}
publish {
    name = "pg-annotations"
    groupId = rootProject.groupId
    artifactId = "pg-annotations"
    version = "1.0.0"
    website = "https://github.com/JamesfChen/bundles-assembler"
}
apply plugin: 'java-library'
java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}

